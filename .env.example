# Database Configuration
DATABASE_URL=postgresql+asyncpg://bgstm:bgstm@db:5432/bgstm
# For local development with SQLite: sqlite+aiosqlite:///./bgstm.db

# PostgreSQL Configuration (for docker-compose)
POSTGRES_USER=bgstm
POSTGRES_PASSWORD=bgstm
POSTGRES_DB=bgstm
POSTGRES_PORT=5432

# Backend Configuration
BACKEND_PORT=8000
API_V1_PREFIX=/api/v1
PROJECT_NAME=BGSTM AI Traceability
VERSION=2.0.0
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000"]

# Python Configuration
PYTHONPATH=/app

# LLM Embedding Configuration
# OPENAI_API_KEY=your_openai_key_here  # Required for OpenAI embeddings (optional)
# AUTO_SUGGESTIONS_ALGORITHM=llm  # Can use 'tfidf', 'keyword', 'hybrid', or 'llm'
