# Legacy Template Links

The canonical BGSTM template directory is [`docs/test-templates/`](../test-templates/README.md).

This directory is retained only so older documentation and external bookmarks using `docs/templates/...` continue to resolve. New documentation must link to `docs/test-templates/...`.
